{
  "name": "toptrandingrepo-e2e",
  "private": true,
  "description": "End-to-end tests for the top-10 trending repos static site (no app build needed — serves public/ directly).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
